# Local only. The deployed site never runs discovery — it serves the committed
# JSON in /data. These variables only affect `npm run populate` on your machine.

# Optional: improves YouTube highlight discovery accuracy during local populate.
# YOUTUBE_API_KEY=
